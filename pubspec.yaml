name: employee
description: "A new Flutter project."
publish_to: 'none' # Remove if you wish to publish to pub.dev

version: 1.0.0+1

environment:
  sdk: ^3.5.0

dependencies:
  flutter:
    sdk: flutter
  cupertino_icons: ^1.0.8
  file_picker: ^5.0.2
  bloc: ^8.1.4
  equatable: ^2.0.5
  sqflite: ^2.0.0+3
  sqflite_common_ffi: ^2.0.0+3
  sqlite3_flutter_libs: ^0.5.26
  shared_preferences: ^2.3.3
  hive: ^2.2.3
  hive_flutter: ^1.1.0  # Correct Hive version compatibility
  build_runner: ^2.3.3
  hive_generator: ^1.1.0
  url_launcher: ^6.1.5  # Make sure this is compatible with your Flutter version
  url_launcher_windows: ^3.1.3 # Ensure version is appropriate for Flutter 
  path: ^1.8.0

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^4.0.0

flutter:
  uses-material-design: true
